name: postgame

services:
  postgres:
    image: postgres
    container_name: postgame-postgres
    environment:
      POSTGRES_DB: postgame
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
